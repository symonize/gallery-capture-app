# Optional: prefill keys for local dev instead of typing them in Settings.
# These are still exposed in the client bundle — see README security note.
VITE_OPENAI_API_KEY=
VITE_ANTHROPIC_API_KEY=
VITE_AIRTABLE_TOKEN=
VITE_AIRTABLE_BASE_ID=

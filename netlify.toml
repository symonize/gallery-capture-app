[build]
  command = "pnpm build"
  publish = "dist"

# SPA fallback: serve index.html for client-side routes so deep links / refresh work.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
